---
description: Decompose a project into independently implementable features with dependency mapping. Use when breaking down work for the Plan phase.
allowed-tools: Read, Grep, Glob, Write, Edit, Bash
---

Break the solution into discrete features (200-500 lines each). Map dependencies between features. Build a feature DAG in JSON format. Create per-feature specifications with acceptance criteria.
